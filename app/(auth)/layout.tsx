const AuthLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className="w-full h-full bg-gray-800 flex justify-center align-center items-center">{children}</div>;
};

export default AuthLayout;
